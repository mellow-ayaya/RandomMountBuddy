# Basic .pkgmeta file
package-as: RandomMountBuddy  # Replace with your addon's folder name

# Use this to provide a custom changelog instead of commit messages
manual-changelog: CHANGELOG.md

# Optional: Ignore files you don't want packaged
ignore:
  - README.md
  - _RMB tools and misc
